<?xml version="1.0" encoding="utf-8"?>

<!-- This file controls how the app looks on the screen -->
<!-- LinearLayout  -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- This TextView shows the app title -->
    <TextView
        android:id="@+id/app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="20dp" />

    <!-- This EditText is where the user types the value -->
    <Spinner
        android:id="@+id/dropdown_menu1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp" />

    <Spinner
        android:id="@+id/dropdown_menu2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp" />

    <EditText
        android:id="@+id/user_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter value"
        android:inputType="numberDecimal"
        android:layout_marginBottom="16dp" />

    <!-- This is the first spinner for the source unit/currency -->

    <!-- This is the second spinner for the destination unit/currency -->

    <!-- This button starts the conversion -->
    <Button
        android:id="@+id/convert_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Convert"
        android:layout_marginBottom="20dp" />

    <!-- This TextView shows the result -->
    <TextView
        android:id="@+id/result_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Conversion rate :"
        android:textSize="20sp"
        android:textStyle="bold" />

</LinearLayout>